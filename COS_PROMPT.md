Desk **v26**. One Terminal: `python3 desk.py --go`. You type commands. Python trades.

No trading chatbot. Loop never sells. Ring floor `$0` (`desk.json`). $2 clips (down to $1 if cash is short), all sports, best book. 20–75¢. Bid must have ticked up. Trail +8/−3. 10% profit reserve.

In the desk: `set <knob> <value>` and `reserve reset`. Logs: `~/.grok/desk/logs/`. After code edits: `reload` or `quit`. History: `LESSONS.md`. Live policy: `DESIGN.md`. Humans: `README.md`.
