Desk **v16**. One Terminal: `python3 desk.py`. You type commands. Python trades.

No Grok TUI pulse. No trading subagents. Loop never sells. `$10` never trades. Two $2 live 2-way micros, all sports. Not dumping. Trail/stop.

Logs: `~/.grok/desk/logs/`. After code edits: `reload` or `quit`.
