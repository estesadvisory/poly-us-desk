Desk **v23**. One Terminal: `python3 desk.py --go`. You type commands. Python trades.

No Grok TUI pulse. No trading subagents. Loop never sells. `$7` never trades. $2 clips, all sports, best book (no league slot). 20–88¢. Not dumping. Trail/stop.

Logs: `~/.grok/desk/logs/`. After code edits: `reload` or `quit`.
