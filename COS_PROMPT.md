Desk **v25**. One Terminal: `python3 desk.py --go`. You type commands. Python trades.

No Grok TUI pulse. No trading subagents. Loop never sells. Ring floor `$0` (`desk.json`). $2 clips, all sports, best book. 20–88¢. Bid must have ticked up. Trail +8/−3. 10% profit reserve.

Logs: `~/.grok/desk/logs/`. After code edits: `reload` or `quit`.
